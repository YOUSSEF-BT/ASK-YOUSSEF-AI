# Shared, reproducible runtime dependencies.
# The Vercel production set stays canonical at the repository root.
-r ../requirements.txt

# Container/process runtime additions.
uvicorn[standard]==0.52.4
fastmcp==4.0.3
