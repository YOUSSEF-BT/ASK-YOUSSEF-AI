# Reproducible Vercel production dependency set for Python 3.12.
# Dependabot monitors these exact direct pins; CI audits the resolved dependency
# graph with pip-audit before changes are accepted.
fastapi==0.141.1
pydantic==2.13.5
numpy==2.5.3
google-genai==2.23.0
fastembed==0.8.0
