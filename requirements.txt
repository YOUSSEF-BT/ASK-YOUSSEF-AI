# Vercel production runtime for Ask Youssef AI.
# FastEmbed provides local dense vector retrieval, while Gemini is reserved for
# answer generation. This avoids consuming Gemini embedding quota on cold starts.
fastapi>=0.110
pydantic>=2.0
numpy>=1.24
google-genai>=2.0
fastembed>=0.6.1
